tvmcc<-function(dados)
{
alpha<-as.numeric(readline("Informe o nível de significância desejado:  "))
readline("Para entrar com a hipótese nula tecle enter!")
mu_zero<-edit(matrix(0,ncol(dados),1))
readline("Para entrar com a matriz de covariâncias tecle enter!")
sigma<-edit(diag(ncol(dados)))
est<-nrow(dados)*t(mean(dados)-mu_zero)%*%solve(sigma)%*%(mean(dados)-mu_zero)
cat("O valor da estatística de teste é:",est,".",fill=T)
qui<-qchisq(alpha,ncol(dados),lower.tail=F)
cat("O valor crítico delimitador da região de rejeição considerando",alpha," é:",qui, fill=T)
pr.f = 1 - pchisq(est,ncol(dados))
cat("valor-p:",pr.f,".",fill=T)
}
#testando
#dados<-read.table("dados_exemplo.txt",header=T)
#j<-tvmcc(dados)
